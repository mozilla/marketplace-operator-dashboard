define('init',
    ['core/init', 'helpers_local', 'routes', 'settings_app'],
    function(init, helpersLocal, routes, settingsApp) {
    // Put any code that needs to run to initialize the app here or in the
    // dependencies.
});
